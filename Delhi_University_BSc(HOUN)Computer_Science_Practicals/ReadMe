[+] To run .sql file directly in [Linux].
      STEP-1 : Open Terminal.
      STEP-2 : Type command:
                          service mysql start;
      STEP-3 : Now,type command:
                          mysql -u root -p;
                          Then, just press Enter or Password if any.
      STEP-4 : Now,
                  type : source /file_path/filename.sql
                  And hit Enter.
[+] If you have [lampp] installed, then
      STEP-1 : Open Terminal.
      STEP-2 : Type command:
                          sudo /opt/lampp/lampp startmysql;
      STEP-3 : Now,type command:
                          sudo /opt/lampp/bin/mysql -u root -p;
                          Then, just press Enter or Password if any.
      STEP-4 : Now,
                  type : source /file_path/filename.sql
                  And hit Enter.
[+] To run .sql file directly in [Windows].
      STEP-1 : Open MySQL or any other RDBMS.
      STEP-2 : Then, just press Enter or Password if any.
      STEP-4 : Now,
                  type : source /file_path/filename.sql
                  And hit Enter.
